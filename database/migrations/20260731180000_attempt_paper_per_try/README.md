# Attempt paper per try

Allows a new deterministic paper snapshot for each attempt. Fixed assessments
retain their published question versions, while random assessments can draw a
fresh seeded paper for every permitted try as required by OneXL.

The migration also adds a JSON result summary for compatibility metrics such as
accuracy, detection rate, pending-review state, and late submission.
